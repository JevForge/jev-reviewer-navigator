# Contributing

1. Use Node.js 24+.
2. `npm ci`
3. `npm run all` (typecheck, coverage, build)
4. Commit updated `dist/` when sources change.
5. Prefer small PRs with tests for schema, collectors, policy, and executors.

Public docs and runtime messages are English. Conversations with maintainers may be Portuguese.
